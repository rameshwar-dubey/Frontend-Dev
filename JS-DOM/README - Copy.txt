JS Tasks Solutions
------------------
Open index.html in a browser. Each task is implemented on the same page.
JavaScript files are: task1.js ... task9.js (one per question), styles.css for styling.

Notes:
- No frameworks used.
- Uses core HTML/CSS/ES6 JavaScript.
- For the gallery, images are loaded from picsum.photos (internet required to view larger images).
